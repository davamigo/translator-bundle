{##
 # DavamigoTranslatorBundle:Default:layout.html.twig
 # @DavamigoTranslator/Default/layout.html.twig
 #}

{% use "bootstrap_3_layout.html.twig" %}

{% trans_default_domain 'DavamigoTranslatorBundle' %}

{% block form_errors -%}
    {% if errors|length > 0 -%}
        {% if form.parent %}<span class="help-block">{% else %}<div class="alert alert-danger">{% endif %}
        <ul class="list-unstyled">
            {%- for error in errors -%}
                <li><span class="glyphicon glyphicon-exclamation-sign"></span> {{ error.message | trans }}</li>
            {%- endfor -%}
        </ul>
        {% if form.parent %}</span>{% else %}</div>{% endif %}
    {%- endif %}
{%- endblock form_errors %}
